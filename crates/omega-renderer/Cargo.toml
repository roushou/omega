[package]
name = "omega-renderer"
version.workspace = true
categories = ["gui", "os::linux-apis"]
edition.workspace = true
homepage.workspace = true
keywords = ["omarchy", "quickshell", "qml", "wayland", "widget"]
license.workspace = true
readme = "README.md"
repository.workspace = true
rust-version.workspace = true
description = "The QML Omega ships to draw the view trees its units publish, and how it installs."

[lints]
workspace = true

[dependencies]
anyhow = "1"
omega-host.workspace = true
omega-proto.workspace = true
serde_json.workspace = true

[dev-dependencies]
