syntax = "proto3";

package omega;

// Generic values for plugin records, node properties, settings, and command input.
// SDK accessors validate and convert values at input boundaries.

message Value {
  oneof kind {
    bool bool_value = 1;
    int64 int_value = 2;
    double double_value = 3;
    string string_value = 4;
    bytes bytes_value = 5;
    ListValue list = 6;
    MapValue map = 7;
    Color color = 8;
    Image image = 9;
  }
}

message ListValue {
  repeated Value values = 1;
}

message MapValue {
  map<string, Value> entries = 1;
}

message Color {
  float r = 1;
  float g = 2;
  float b = 3;
  float a = 4;   // 0.0 .. 1.0
}

message Image {
  bytes data = 1;
  string mime = 2;        // "image/png", ...
  uint32 width = 3;
  uint32 height = 4;
  uint32 stride = 5;
}
